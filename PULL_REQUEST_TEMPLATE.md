## What

<!-- Task card (for example A1.3) or issue, and a short summary of the change -->

## Checklist

- [ ] Contract changes went to `docs/detailed-design/` first (both `X.md` and `X.vi.md`; validator `problems=0`, bilingual check passes) and to `shared/` before code
- [ ] Tests pass — commands and results below
- [ ] Small Conventional Commits, `git commit -s`, under a real name
- [ ] No secrets, keys, certificates or dotenv files
- [ ] UI text comes from the string catalog in English and Vietnamese; components follow the design system
- [ ] Changes to `shared/` are called out so the other platforms re-run their tests
